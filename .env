VITE_API_URL=http://localhost:8000/api/v1
VITE_APP_NAME=JobTrack AI
VITE_APP_VERSION=1.0.0
# enable dev-only auth bypass (use VITE_SKIP_AUTH since React prefix isn't available in Vite)
VITE_SKIP_AUTH=true
# REACT_APP_SKIP_AUTH=true  # kept only for reference/backwards compatibility
